export enum SlotStatus {
    AVAILABLE = "available",
    BOOKED = "booked",
    ACTIVE = "active",
    COMPLETED = "completed",
    CANCELLED = "cancelled"
}

export enum SessionType {
    ONE_ON_ONE = "one_on_one",
    GROUP = "group"
}