export enum SlotStatus {
    AVAILABLE = "available",
    BOOKED = "booked",      
    COMPLETED = "completed",
    CANCELLED = "cancelled"
}

export enum SessionType {
    ONE_ON_ONE = "one_on_one",
    GROUP = "group"
}